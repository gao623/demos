SET(DEST_FILE hellobin)
SET(SRC_LIST main.c)
ADD_EXECUTABLE(${DEST_FILE} ${SRC_LIST})

SET_TARGET_PROPERTIES(
    ${DEST_FILE} PROPERTIES
            RUNTIME_OUTPUT_DIRECTORY "${PROJECT_SOURCE_DIR}/bin"
)
