CMAKE_MINIMUM_REQUIRED(VERSION 2.8)

PROJECT(HELLO)

INCLUDE_DIRECTORIES(
    ${PROJECT_SOURCE_DIR}/include
)

MESSAGE(STATUS "This is BINARY DIR " ${PROJECT_BINARY_DIR})
MESSAGE(STATUS "This is SOURCE DIR " ${PROJECT_SOURCE_DIR})

# if set EXECUTABLE_OUTPUT_PATH, or LIBRARY_OUTPUT_PATH
# use ADD_EXECUTABLE (unuse SET_TARGET_PROPERTIES)
# all executable file will be output to this path
# use ADD_LIBRARY (unuse SET_TARGET_PROPERTIES)
# all library file will be output to this path
SET(EXECUTABLE_OUTPUT_PATH ${PROJECT_BINARY_DIR}/bin)
SET(LIBRARY_OUTPUT_PATH ${PROJECT_BINARY_DIR}/lib)
#SET(EXECUTABLE_OUTPUT_PATH ${PROJECT_SOURCE_DIR}/bin)
#SET(LIBRARY_OUTPUT_PATH ${PROJECT_SOURCE_DIR}/lib)

ADD_SUBDIRECTORY(src)
ADD_SUBDIRECTORY(tests)
ADD_SUBDIRECTORY(unittests)
